import { Send, Square } from "lucide-react";
import { useRef, useState, type FormEvent, type KeyboardEvent } from "react";
import { Button } from "@/components/ui/button";
export function Composer({streaming,onSend,onStop}:{streaming:boolean;onSend:(text:string)=>void;onStop:()=>void}) { const [value,setValue]=useState(""); const ref=useRef<HTMLTextAreaElement>(null); function submit(){const t=value.trim();if(!t||streaming)return;onSend(t);setValue("");if(ref.current)ref.current.style.height="auto";} function key(e:KeyboardEvent<HTMLTextAreaElement>){if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();submit();}} return <form onSubmit={(e:FormEvent)=>{e.preventDefault();submit()}} className="border-t border-border bg-bg/90 px-3 pt-3 pb-[max(0.75rem,env(safe-area-inset-bottom))] backdrop-blur-sm sm:px-6"><div className="mx-auto flex w-full max-w-2xl items-end gap-2"><textarea id="message" ref={ref} rows={1} value={value} placeholder="Message Sarah Joe" onChange={e=>{setValue(e.target.value);e.currentTarget.style.height="auto";e.currentTarget.style.height=`${Math.min(e.currentTarget.scrollHeight,160)}px`}} onKeyDown={key} className="max-h-40 min-h-11 flex-1 resize-none rounded-xl border border-border bg-elevated px-4 py-2.5 text-base text-fg placeholder:text-subtle focus-visible:ring-accent/70 focus-visible:ring-2 focus-visible:outline-none"/>{streaming?<Button type="button" variant="quiet" size="icon" onClick={onStop} aria-label="Stop"><Square className="size-4 fill-current"/></Button>:<Button type="submit" size="icon" disabled={!value.trim()} aria-label="Send"><Send className="size-5"/></Button>}</div></form>; }
